'use client';
import SectionDetailsPage from '@/pages/section-details/section-details.page';

const SectionDetails = () => {
  return <SectionDetailsPage id={'-1'} isRecommended={true} />;
};

export default SectionDetails;
